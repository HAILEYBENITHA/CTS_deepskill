# Eureka Client

Microservice that registers itself with the Eureka discovery server. Runs on port 8085.

Run: `cd eureka-client && mvn spring-boot:run`
After starting, the instance appears in the Eureka dashboard at http://localhost:8761